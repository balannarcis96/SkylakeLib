static_assert( sizeof( void* ) == 8, "64bit build Only!" );

#include <cstdint>
#include <type_traits>
#include <memory>
#include <utility>
#include <chrono>
#include <limits>
#include <fstream>
#include <atomic>
